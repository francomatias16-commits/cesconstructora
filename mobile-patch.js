/**
 * CES Construcciones — Mobile Patch v6.0
 *
 * Esta funcionalidad fue consolidada en mobile-video-optimizer.js v8.0.
 * Este archivo se mantiene vacío para no romper la referencia en el HTML.
 *
 * mobile-video-optimizer.js v8 ya cubre:
 *   - Fallback canplaythrough en CADA visita al slide (sin guard _cesPatched)
 *   - Bloqueo de scroll-hijack correcto (antes de registrar el evento)
 *   - Restauración de src si fue eliminado
 *   - Fix de nav scroll con MutationObserver
 *   - Reload en cambio de orientación
 */
